pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('Compilar aplicación') {
            steps {
                sh './gradlew bootJar'
            }
        }

        //stage('Ejecutar pruebas') {
          //  steps {
            //    sh './gradlew test'
           // }
       // }
    } 
} 
