pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('Compilar aplicación') {
            steps {
                sh "${CNS}"
            }
        }

        //stage('Ejecutar pruebas') {
          //  steps {
            //    sh './gradlew test'
           // }
       // }
    } 
} 
